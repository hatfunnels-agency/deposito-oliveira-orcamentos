.claude/

# lixo do macOS
.DS_Store

# dependencias e build
node_modules/
.next/
next-env.d.ts
tsconfig.tsbuildinfo

# variaveis de ambiente — NUNCA versionar.
# .env.example fica de fora do ignore de proposito: ele so tem nomes, sem valores.
.env
.env.local
.env.*.local
.env.development
.env.production

# extratos bancarios e financeiro sensivel — NUNCA versionar.
# O repositorio e publico. Extrato tem saldo, CPF/CNPJ de fornecedor e
# historico de movimentacao; nada disso pode sair da maquina.
/Financeiro/
*.ofx
/extratos/

# pastas auxiliares (nao fazem parte do app)
mcp-gmn/
historico/
brand-guidelines/
